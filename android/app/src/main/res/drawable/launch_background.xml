<?xml version="1.0" encoding="utf-8"?>
<!-- Native pre-Flutter splash frame. Matches the brand gradient + mark that
     lib/screens/splash_screen.dart fades in next, so there's no jarring
     white flash between process start and the first Flutter frame. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:type="linear"
                android:angle="315"
                android:startColor="#FFF07E5C"
                android:centerColor="#FFEF6B7D"
                android:endColor="#FF7C6BD6"/>
        </shape>
    </item>
    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/ic_launcher_foreground" />
    </item>
</layer-list>
