<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Groq AI calls — the app's only network dependency, opt-in via Settings -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Notifications (Android 13+ requires runtime permission) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- Full-screen intent: task alarm pops on lock screen like a real alarm clock -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>

    <!-- Task alarms: exact-time ringing, reboot survival, foreground playback -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <!-- Task alarms: many OEMs (MIUI, ColorOS, FuntouchOS, One UI, ...) kill
         background work for apps under standard battery optimization even
         with every other alarm/notification permission granted. This lets
         Settings offer the "unrestricted battery usage" exemption an actual
         alarm-clock app needs to fire reliably while the screen is off. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <!-- Smart Reminders: brief background check-ins that call Groq and act -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>

    <application
        android:label="InkList"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Task alarms: fires when AlarmManager wakes the device -->
        <receiver android:name=".AlarmReceiver" android:exported="false"/>

        <!-- Task alarms: AlarmManager entries don't survive reboot, so re-arm them -->
        <receiver android:name=".BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <!-- Task alarms: loops the ringtone + vibration while the alarm is live -->
        <service
            android:name=".AlarmRingingService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"/>

        <!-- Task alarms: full-screen ringing UI, shows over the lock screen -->
        <activity
            android:name=".AlarmRingingActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:theme="@style/AlarmActivityTheme"/>

        <!-- Smart Reminders: fires at each of the daily check-in times -->
        <receiver android:name=".SmartReminderReceiver" android:exported="false"/>

        <!-- Smart Reminders: reads tasks, calls Groq, decides notify/alarm/none -->
        <service
            android:name=".SmartReminderService"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>

        <!-- Home-screen widget: today's task count + top 3 open tasks -->
        <receiver android:name=".InkListWidgetProvider" android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/inklist_widget_info"/>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
